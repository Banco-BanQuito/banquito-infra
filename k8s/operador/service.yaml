apiVersion: v1
kind: Service
metadata:
  name: operador-frontend
  namespace: banquito-frontend
  labels:
    app: operador
spec:
  type: ClusterIP
  selector:
    app: operador
  ports:
    - name: http
      port: 8080
      targetPort: 8080


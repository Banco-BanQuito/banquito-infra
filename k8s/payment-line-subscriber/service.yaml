apiVersion: v1
kind: Service
metadata:
  name: payment-line-subscriber-service
  namespace: banquito-switch
  labels:
    app: payment-line-subscriber
spec:
  type: ClusterIP
  selector:
    app: payment-line-subscriber
  ports:
    - name: http
      port: 8091
      targetPort: 8091

apiVersion: v1
kind: Service
metadata:
  name: teller-frontend
  namespace: banquito-frontend
  labels:
    app: teller
spec:
  type: ClusterIP
  selector:
    app: teller
  ports:
    - name: http
      port: 8080
      targetPort: 8080


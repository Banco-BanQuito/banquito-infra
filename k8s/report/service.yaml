apiVersion: v1
kind: Service
metadata:
  name: report-service
  namespace: banquito-switch
  labels:
    app: report
spec:
  type: ClusterIP
  selector:
    app: report
  ports:
    - name: http
      port: 8088
      targetPort: 8088


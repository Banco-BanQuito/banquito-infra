apiVersion: v1
kind: Service
metadata:
  name: tariff-service
  namespace: banquito-switch
  labels:
    app: tariff
spec:
  type: ClusterIP
  selector:
    app: tariff
  ports:
    - name: http
      port: 8086
      targetPort: 8086
    - name: grpc
      port: 9090
      targetPort: 9090


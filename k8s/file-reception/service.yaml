apiVersion: v1
kind: Service
metadata:
  name: file-reception-service
  namespace: banquito-switch
  labels:
    app: file-reception
spec:
  type: ClusterIP
  selector:
    app: file-reception
  ports:
    - name: http
      port: 8084
      targetPort: 8084

